fn main() {
    println!("Welcome to Rusted Calculator, a Reverse Polish Notation calculator");

    // 0. Parse the command-line arguments
    
    // 1. Parse out the command and arguments
    
    // 2. Run the parsed command with given arguments
    
    // 3. Return the result
}

/// The add function; takes any two numerical types and adds them together
fn add() {}
